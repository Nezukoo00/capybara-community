# 📖 Полная документация Капибара Сообщества

## 🏗️ Архитектура проекта

```
КАПИБАРА СООБЩЕСТВО
│
├─ Базовые страницы
│  ├─ capybara-community.html (Главная)
│  ├─ capybara-about.html (О капибарах)
│  ├─ capybara-login.html (Вход)
│  ├─ capybara-register.html (Регистрация)
│  └─ capybara-profile.html (Профиль)
│
├─ JavaScript
│  └─ capybara-script.js (Вся логика приложения)
│
└─ Документация
   ├─ README.md (Основной гайд)
   ├─ QUICK_START.md (Быстрый старт)
   └─ DOCUMENTATION.md (Этот файл)
```

## 🗄️ Структура данных localStorage

### 1. Пользователи (`users`)
```json
[
  {
    "id": "1234567890",
    "username": "капибара_123",
    "email": "capybara@example.com",
    "password": "password123",
    "joinDate": "2025-01-15T10:30:00.000Z"
  }
]
```

**Поля:**
- `id` - уникальный идентификатор (временная метка)
- `username` - имя пользователя (3+ символов)
- `email` - электронная почта (уникальный)
- `password` - пароль (6+ символов)
- `joinDate` - дата регистрации (ISO 8601)

### 2. Комментарии (`comments`)
```json
[
  {
    "id": "1234567891",
    "userId": "1234567890",
    "username": "капибара_123",
    "text": "Капибары замечательные!",
    "date": "2025-01-15T11:00:00.000Z",
    "page": "home"
  }
]
```

**Поля:**
- `id` - уникальный ID комментария
- `userId` - ID автора комментария
- `username` - имя автора (для быстрого доступа)
- `text` - содержание комментария (до 500 символов)
- `date` - дата и время публикации
- `page` - на какой странице опубликовано ("home" или "about")

### 3. Текущий пользователь (`currentUser`)
```json
{
  "id": "1234567890",
  "username": "капибара_123",
  "email": "capybara@example.com",
  "joinDate": "2025-01-15T10:30:00.000Z"
}
```

⚠️ **Примечание:** Поле `password` не сохраняется в currentUser в целях безопасности

## 🔧 JavaScript API

### Управление пользователями

#### `register(username, email, password)`
Регистрирует нового пользователя.

```javascript
// Возвращает: true (успех) или false (ошибка)
const result = register('капибара', 'capybara@email.com', 'password123');

if (result) {
  console.log('Регистрация успешна!');
} else {
  console.log('Пользователь уже существует');
}
```

**Валидация:**
- Имя: минимум 3 символа
- Email: уникален в системе
- Пароль: минимум 6 символов

**Побочные эффекты:**
- Автоматический вход в аккаунт
- Сохранение в localStorage

---

#### `login(email, password)`
Входит в аккаунт пользователя.

```javascript
// Возвращает: true (успех) или false (ошибка)
const result = login('capybara@email.com', 'password123');

if (result) {
  window.location.href = 'capybara-community.html';
}
```

**Валидация:**
- Email существует в системе
- Пароль совпадает

---

#### `logout()`
Выходит из аккаунта.

```javascript
logout(); // Перенаправляет на главную страницу
```

---

#### `deleteAccount()`
Удаляет аккаунт пользователя (с подтверждением).

```javascript
deleteAccount(); // Удаляет аккаунт после подтверждения
```

**Шаги:**
1. Проверяет авторизацию
2. Просит двойное подтверждение (чтобы избежать случайного удаления)
3. Удаляет пользователя из хранилища
4. Выходит из аккаунта
5. Перенаправляет на главную страницу

**Возвращает:** `true` (если удалено) или `false` (если отменено)

**Важно:**
- Комментарии пользователя остаются в системе
- Это действие НЕОБРАТИМО
- Требует двойного подтверждения для безопасности

---

#### `getCurrentUser()`
Получает информацию о текущем пользователе.

```javascript
const user = getCurrentUser();

if (user) {
  console.log(user.username);  // Имя пользователя
  console.log(user.email);     // Email
  console.log(user.id);        // ID
  console.log(user.joinDate);  // Дата присоединения
}
```

**Возвращает:**
- Объект пользователя (если авторизован)
- `null` (если не авторизован)

---

#### `isLoggedIn()`
Проверяет, вошел ли пользователь в аккаунт.

```javascript
if (isLoggedIn()) {
  // Показать элементы для авторизованного пользователя
  document.getElementById('comment-form').style.display = 'block';
} else {
  // Показать приглашение войти
  document.getElementById('login-prompt').style.display = 'block';
}
```

**Возвращает:** `true` или `false`

---

#### `updateAuthUI()`
Обновляет интерфейс в зависимости от статуса авторизации.

```javascript
updateAuthUI(); // Автоматически показывает/скрывает элементы
```

**Что обновляется:**
- Кнопки "Войти" / "Зарегистрироваться"
- Информация о пользователе в навбаре
- Видимость формы комментариев

### Управление комментариями

#### `submitComment(page)`
Публикует новый комментарий.

```javascript
submitComment('home');  // На главной странице
submitComment('about'); // На странице "О капибарах"
```

**Шаги:**
1. Получает текст из textarea с ID `comment-text`
2. Проверяет авторизацию
3. Сохраняет комментарий
4. Очищает форму
5. Перезагружает ленту комментариев

**Требует:**
- Авторизацию
- Непустой текст
- Максимум 500 символов

---

#### `loadComments(page)`
Загружает и отображает комментарии страницы.

```javascript
loadComments('home');   // Загрузить комментарии главной
loadComments('about');  // Загрузить комментарии страницы О капибарах
```

**Что делает:**
1. Получает все комментарии со страницы
2. Сортирует по дате (новые первыми)
3. Форматирует вывод
4. Отображает кнопку удаления для своих комментариев
5. Показывает сообщение если комментариев нет

---

#### `getAllComments()`
Получает все комментарии со всех страниц.

```javascript
const allComments = getAllComments();
console.log(allComments.length); // Общее количество
```

**Возвращает:** Массив всех комментариев

---

#### `deleteComment(commentId, page)`
Удаляет комментарий (с подтверждением).

```javascript
deleteComment('1234567891', 'home');
```

**Шаги:**
1. Просит подтверждение пользователя
2. Удаляет комментарий из хранилища
3. Перезагружает ленту

---

#### `removeComment(commentId)`
Удаляет комментарий без подтверждения (внутренняя функция).

```javascript
removeComment('1234567891'); // Прямое удаление
```

---

### Утилиты

#### `escapeHtml(text)`
Экранирует HTML для безопасности.

```javascript
const safe = escapeHtml('<script>alert("XSS")</script>');
// Результат: &lt;script&gt;alert(&quot;XSS&quot;)&lt;/script&gt;
```

## 📊 Жизненный цикл

### При загрузке страницы:
```
1. DOMContentLoaded
   ↓
2. initUsers()        - Инициализация хранилища пользователей
   ↓
3. initComments()     - Инициализация хранилища комментариев
   ↓
4. updateAuthUI()     - Обновление интерфейса
   ↓
5. loadComments()     - Загрузка комментариев страницы
```

### При регистрации:
```
1. Заполнение формы
   ↓
2. Валидация данных
   ↓
3. register()
   ↓
4. login() (автоматический)
   ↓
5. Перенаправление
```

### При публикации комментария:
```
1. Нажатие "Отправить"
   ↓
2. submitComment()
   ↓
3. Валидация
   ↓
4. Сохранение в localStorage
   ↓
5. loadComments()
   ↓
6. Вывод в ленту
```

## 🔐 Безопасность

### Реализовано:
✅ Валидация входных данных
✅ Экранирование HTML (escapeHtml)
✅ Проверка авторизации
✅ Максимальная длина текста (500 символов)

### НЕ реализовано (требует сервера):
❌ Хеширование пароля
❌ HTTPS
❌ CSRF защита
❌ Rate limiting
❌ Проверка на стороне сервера

## 🎨 CSS переменные

```css
--primary-color: #8b7355;     /* Основной коричневый */
--secondary-color: #d4a574;   /* Вторичный коричневый */
--accent-color: #f5deb3;      /* Акцентный бежевый */
--dark-color: #3a3a3a;        /* Темный текст */
--light-color: #faf8f3;       /* Светлый фон */
--success-color: #4caf50;     /* Зеленый */
--error-color: #f44336;       /* Красный */
--border-radius: 12px;        /* Скругление */
```

## 📱 Медиа-запросы

```css
@media (max-width: 768px) {
  /* Адаптация для мобильных устройств */
}
```

## 🚀 Производительность

**Оптимизации:**
- Минимальное количество DOM операций
- Кеширование элементов
- Локальное хранилище вместо сетевых запросов
- Встроенные стили (нет внешних CSS файлов)

**Примерные размеры:**
- HTML файлы: 8-12 KB каждый
- JavaScript: 7 KB
- Общий размер: ~70 KB

## 🧪 Примеры использования

### Пример 1: Создание формы входа
```javascript
document.getElementById('login-form').addEventListener('submit', (e) => {
  e.preventDefault();
  
  const email = document.getElementById('email').value;
  const password = document.getElementById('password').value;
  
  if (login(email, password)) {
    window.location.href = 'capybara-community.html';
  } else {
    alert('Неверные учетные данные');
  }
});
```

### Пример 2: Проверка авторизации перед входом на страницу
```javascript
document.addEventListener('DOMContentLoaded', () => {
  if (!isLoggedIn()) {
    window.location.href = 'capybara-login.html';
  }
});
```

### Пример 3: Фильтрация комментариев по пользователю
```javascript
const allComments = getAllComments();
const currentUser = getCurrentUser();
const myComments = allComments.filter(c => c.userId === currentUser.id);

console.log(`У вас ${myComments.length} комментариев`);
```

## 🐛 Отладка

### Просмотр localStorage:
```javascript
// В консоли браузера (F12)
console.log(JSON.parse(localStorage.getItem('users')));
console.log(JSON.parse(localStorage.getItem('comments')));
console.log(JSON.parse(localStorage.getItem('currentUser')));
```

### Очистка данных:
```javascript
// Удалить все пользователей
localStorage.removeItem('users');

// Удалить все комментарии
localStorage.removeItem('comments');

// Удалить текущего пользователя (выход)
localStorage.removeItem('currentUser');

// Полная очистка
localStorage.clear();
```

## 📈 Возможные улучшения

### Frontend:
- [x] Добавить удаление аккаунта
- [ ] Редактирование профиля
- [ ] Загрузка аватаров
- [ ] Поиск по комментариям
- [ ] Фильтрация по датам
- [ ] Лайки на комментарии
- [ ] Ответы на комментарии
- [ ] Темная тема

### Backend (требуется сервер):
- [ ] Express/Node.js сервер
- [ ] PostgreSQL/MongoDB база данных
- [ ] Хеширование паролей (bcrypt)
- [ ] JWT токены для авторизации
- [ ] API endpoints
- [ ] Вход через Google/GitHub
- [ ] Email подтверждение
- [ ] Восстановление пароля

## 📞 Контакты и поддержка

Если у вас есть вопросы:
1. Проверьте консоль браузера (F12) на ошибки
2. Посмотрите QUICK_START.md
3. Прочитайте README.md

---

**Хорошего времени с капибарами!** 🐹❤️
